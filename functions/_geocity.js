/**
 * _geocity.js — განცხადების ქალაქთან მიბმა კოორდინატებით.
 * ==================================================================
 * ⚠️ 2026-09-10, ROOT CAUSE — რატომ გაჩნდა ეს ფაილი.
 *
 * `lst` ცხრილს აქვს ტექსტური სვეტები `loc`/`reg` (ქალაქი/რეგიონი).
 * SEO ლენდინგები (`[type]/[city].js`) და sitemap სწორედ მათზე
 * ეძებდნენ: `WHERE loc LIKE '%თბილისი%' OR reg LIKE '%თბილისი%'`.
 *
 * პრობლემა: ეს ორი სვეტი კლიენტიდან მოდის (`submit.js`: `o.loc =
 * str(b.loc,120) || null`), ფორმა კი მათ არ აგზავნის — ანუ **ყველა
 * აქტიურ განცხადებაზე `loc` და `reg` NULL-ია** (გადამოწმებულია
 * ბაზაზე: 34/34). შედეგად:
 *   • ყველა ქალაქის ლენდინგი ცარიელი იყო — `/land-for-sale/tbilisi`
 *     h1-ს ხატავდა, ბმულს კი 0 ცალს. ეს გვერდები მთავარი გვერდის
 *     ფუტერიდანაა დალინკული, ანუ Google-ის ერთ-ერთი მთავარი შესასვლელი;
 *   • sitemap-ის ლენდინგების ბლოკი ყოველთვის 0 URL-ს აბრუნებდა —
 *     ანუ არც ერთი ქალაქის გვერდი არასდროს შესულა sitemap-ში;
 *   • რუკის ბარათებზე მდებარეობის ტექსტი fallback-ზე ვარდებოდა.
 *
 * გამოსავალი: ტექსტს აღარ ვენდობით — ქალაქს **კოორდინატებით**
 * ვარკვევთ. ნიშნული (lat/lng) ფორმაში სავალდებულოა, ანუ ყოველ
 * განცხადებას აქვს. ზუსტად იმავე პრინციპით მუშაობს `/g/[id]`-ის
 * გვერდი უკვე დიდი ხანია (nearestCity, 60 კმ) — აქ ის საერთო,
 * ერთ ადგილას თავმოყრილ ჰელპერად გამოვიტანეთ.
 *
 * რატომ „უახლოესი", და არა უბრალოდ რადიუსი: რადიუსებით ქალაქები
 * ერთმანეთს ფარავს (მცხეთის ნაკვეთი თბილისის 30 კმ-შიც ჯდება), და
 * ერთი და იგივე განცხადება ორ ლენდინგზე გაჩნდებოდა — Google-ისთვის
 * ეს დუბლირებული კონტენტია. „უახლოესი ქალაქი" თითოეულ განცხადებას
 * ზუსტად ერთ გვერდზე ამაგრებს.
 *
 * ⚠️ ეს ჰელპერი მხოლოდ **კითხვის** დროს მუშაობს — ბაზაში არაფერს
 * წერს და მიგრაციას არ საჭიროებს. ამიტომ ის უკვე არსებულ 34
 * განცხადებასაც მაშინვე ეხება, არა მხოლოდ ახლებს.
 */
import { CITY_SLUGS } from './_cities.js';

/* რამდენ კილომეტრამდე ჩავთვალოთ, რომ განცხადება ქალაქს „ეკუთვნის".
   60 კმ იგივე ზღვარია, რასაც `/g/[id]/index.js` იყენებს — საქართველოს
   მასშტაბით ეს დაახლოებით მუნიციპალიტეტის რადიუსია. */
export const CITY_MAX_KM = 60;

/* ჰავერსინი. R = დედამიწის საშუალო რადიუსი კმ-ში. */
export function distKm(lat1, lng1, lat2, lng2) {
  const R = 6371, rad = Math.PI / 180;
  const dLat = (lat2 - lat1) * rad, dLng = (lng2 - lng1) * rad;
  const a = Math.sin(dLat / 2) ** 2 +
    Math.cos(lat1 * rad) * Math.cos(lat2 * rad) * Math.sin(dLng / 2) ** 2;
  return 2 * R * Math.asin(Math.sqrt(a));
}

/* ⚠️ ქალაქის „მოთხოვნის რადიუსი" — რატომ არ გამოდგა უბრალო „უახლოესი".
   CITY_SLUGS-ში ყოველი ქალაქი ერთი წერტილია, ზომის გარეშე. თბილისის
   წერტილი (41.6937, 44.8013) ქალაქის სამხრეთ ნაწილშია, მცხეთა კი
   ჩრდილოეთით მხოლოდ ~17 კმ-ზეა — ამიტომ ბრმა „უახლოესი წერტილი"
   თბილისის ჩრდილო უბნებს (გლდანი, დიღომი, საბურთალო) **მცხეთას**
   აწერდა, ვაჟა-ფშაველას — სოფელ წავკისს. ცოცხალ მონაცემზე
   გადამოწმებულია: 34 აქტიური განცხადებიდან 6 არასწორ ქალაქზე ხვდებოდა.
   ამიტომ მანძილს ქალაქის მასშტაბზე ვყოფთ: იგებს ის, ვისთვისაც
   შეფარდება km/radius ყველაზე მცირეა. დიდ ქალაქს დიდი რადიუსი აქვს,
   სოფელს — პატარა, ანუ სოფელი მხოლოდ მაშინ იგებს, როცა ობიექტი
   ნამდვილად მასთან ახლოსაა. გადამოწმებულია, რომ თბილისის გარეთ
   ვერცერთი მისამართის მიბმა არ შეცვლილა. */
const CITY_RADIUS_KM = { tbilisi: 25, batumi: 16, kutaisi: 16 };
const claimRadius = C => CITY_RADIUS_KM[C[0]] || (C[5] === 1 ? 12 : 9);

/**
 * აბრუნებს ქალაქის ჩანაწერს [slug, ka, en, lat, lng, tier], რომელსაც
 * ეს კოორდინატი ყველაზე მეტად „ეკუთვნის", ან null-ს — თუ ყველა
 * ქალაქი CITY_MAX_KM-ზე შორსაა (ან კოორდინატი არ არსებობს).
 */
export function nearestCity(lat, lng) {
  if (typeof lat !== 'number' || typeof lng !== 'number' ||
      !isFinite(lat) || !isFinite(lng)) return null;
  let best = null, bestScore = Infinity, bestKm = Infinity;
  for (const C of CITY_SLUGS) {
    const km = distKm(lat, lng, C[3], C[4]);
    const score = km / claimRadius(C);
    if (score < bestScore) { bestScore = score; bestKm = km; best = C }
  }
  return bestKm <= CITY_MAX_KM ? best : null;
}

/** მოკლე ჩანაწერი: მხოლოდ slug (ან ''). */
export const nearestCitySlug = (lat, lng) => (nearestCity(lat, lng) || [''])[0];

/**
 * ერთი ქალაქის გარშემო კოორდინატების ჩარჩო (SQL-ის იაფი წინასწარი
 * გაფილტვრისთვის — ზუსტ „უახლოესობას" მერე JS ამოწმებს).
 * გრძედის ერთი გრადუსი საქართველოს განედზე ≈ 0.74 × 111 კმ.
 */
export function cityBox(city, km = CITY_MAX_KM) {
  const lat = city[3], lng = city[4];
  const dLat = km / 111;
  const dLng = km / (111 * Math.cos(lat * Math.PI / 180));
  return { latMin: lat - dLat, latMax: lat + dLat, lngMin: lng - dLng, lngMax: lng + dLng };
}

/**
 * განცხადების ადამიანური მდებარეობა.
 * ჯერ ნამდვილი ტექსტი (loc/reg), თუ ოდესმე შეივსება; თუ არა —
 * უახლოესი ქალაქის სახელი მიმდინარე ენაზე; თუ არც ის — ''.
 */
export function placeLabel(row, lang = 'ka') {
  const txt = [row.loc, row.reg].filter(Boolean).join(', ');
  if (txt) return txt;
  const C = nearestCity(row.lat, row.lng);
  if (!C) return '';
  return lang === 'en' ? C[2] : C[1];
}
